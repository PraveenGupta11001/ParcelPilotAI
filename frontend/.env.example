# ParcelPilot AI - Frontend Environment Guidelines
# In local development, you do not need a .env file as Vite defaults to http://localhost:8000.
# For production deployments (Vercel/Netlify), define this variable in the service provider's dashboard.

VITE_API_URL=http://localhost:8000
